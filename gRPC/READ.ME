Como instalar:

Vai instalar o protoc no linux é:

apt install -y protobuf-compiler

depois roda:
protoc --version
tem que ser maior que 3


Vai no site do gRPC.io

atualmente executa essas duas linhas:

go install google.golang.org/protobuf/cmd/protoc-gen-go@latest
go install google.golang.org/grpc/cmd/protoc-gen-go-grpc@latest



Vai de go mod init pra iniciar o projeto
